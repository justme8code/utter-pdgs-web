'use client';
import Login from "@/app/my_components/Login";

export default function LoginPage() {
  return (
          <main className={"flex h-screen justify-center items-center w-full "}>
              <Login/>
          </main>

  );
}
